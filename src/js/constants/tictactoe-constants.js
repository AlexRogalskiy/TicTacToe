'use strict';

const ADD_MOVE = 'ADD_MOVE';
const RESET = 'RESET';
const START = 'START';
const INITIALIZE = 'INITIALIZE';
const FINALIZE = 'FINALIZE';

module.exports = {
	ADD_MOVE,
	RESET,
	START,
	INITIALIZE,
	FINALIZE
};